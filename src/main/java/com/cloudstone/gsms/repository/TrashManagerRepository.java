package com.cloudstone.gsms.repository;

import com.cloudstone.gsms.mapper.TrashManager;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrashManagerRepository extends JpaRepository<TrashManager,Integer> {
}
